# Browser-download skipping is handled via the PUPPETEER_SKIP_DOWNLOAD=true
# environment variable, set in the `test:screenshot` / `capture` scripts in
# package.json (no deprecated npm config keys here).
#
# The screenshot suite (scripts/capture-screenshots.ts) launches the system
# Chrome/Chromium through PUPPETEER_EXECUTABLE_PATH (or auto-discovery).
