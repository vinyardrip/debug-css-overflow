# No deprecated npm config keys here.
#
# The screenshot suite (scripts/capture-screenshots.ts) is driven by Playwright
# (headless Chromium). It first tries the Playwright-managed browser
# (npx playwright install chromium) and otherwise falls back to the system
# Chrome/Chromium via PLAYWRIGHT_CHROMIUM_EXECUTABLE_PATH (or auto-discovery).
